schema_version: pertura-capability-spec-v2
capability_id: guide.assignment.nb_mixture.v1
version: "0.1.0"
phase: 2
kind: diagnostic
summary: Assign guides with deterministic two-component negative-binomial mixtures.
trust_level: exploratory
executor: guide_nb_mixture
validator: candidate_standard
input_requirements: [guide_matrix]
depends_on: [guide.integrity.v1]
dependency_kinds: [contract, guide_integrity]
output_kind: guide_assignment
source_class: measured_result
claim_permissions: []
timeout_seconds: 1800
parameters_schema:
  type: object
  properties:
    max_memory_gb: {type: number, default: 4.0, exclusiveMinimum: 0}
    n_jobs: {type: integer, default: 1, minimum: 1}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
