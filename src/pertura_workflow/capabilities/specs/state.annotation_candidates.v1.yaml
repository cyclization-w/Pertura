schema_version: pertura-capability-spec-v2
capability_id: state.annotation_candidates.v1
version: 0.1.0
phase: 3
kind: analysis
summary: Generate provenance-bearing candidate labels without replacing technical
  state IDs.
trust_level: exploratory
executor: state_annotation_candidates
validator: candidate_standard
input_requirements:
- state_mapping
depends_on:
- state.reference.map_knn.v1
dependency_kinds:
- contract
- state_mapping
output_kind: state_annotation_candidates
source_class: curated_prior
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    assignment_path:
      type: string
    chunk_rows:
      type: integer
    control_column:
      type: string
    control_values:
      type: array
      items:
        type:
        - string
        - number
        - integer
        - object
    h5ad_path:
      type: string
      x-pertura-asset-role: primary_dataset
      description: Registered asset ID with role primary_dataset.
    manual_labels:
      type: object
    mapping_probability_threshold:
      type: number
    marker_candidates:
      type: object
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    minimum_control_cells:
      type: integer
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    perturbation_labels_used:
      type: boolean
    reference_model_path:
      type: string
    resolutions:
      type: array
      items:
        type:
        - string
        - number
        - integer
        - object
    seeds:
      type: array
      items:
        type:
        - string
        - number
        - integer
        - object
    test_split_used:
      type: boolean
  additionalProperties: false
  examples:
  - {}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
