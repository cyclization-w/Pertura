schema_version: pertura-capability-spec-v2
capability_id: screen.retained_cells.v1
version: 0.1.0
phase: 2
kind: diagnostic
summary: Create a deterministic reason-coded retained-cell manifest.
trust_level: exploratory
executor: retained_cells
validator: candidate_standard
input_requirements:
- guide_assignment
- moi_doublet
depends_on:
- guide.assignment.nb_mixture.v1
- screen.moi_doublet.v1
- guide.ambient.v1
dependency_kinds:
- contract
- guide_assignment
- moi_doublet
- guide_ambient
output_kind: retained_cell_manifest
source_class: measured_result
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    assignment_path:
      type: string
    barcode_column:
      type: string
    chunk_rows:
      type: integer
    design_moi:
      type: string
    doublet_score_column:
      type: string
    doublet_threshold:
      type: number
    filtered_guide_counts_path:
      type: string
      x-pertura-asset-role: guide_matrix
      description: Registered asset ID with role guide_matrix.
    guide_counts_path:
      type: string
      x-pertura-asset-role: guide_matrix
      description: Registered asset ID with role guide_matrix.
    guide_map_path:
      type: string
      x-pertura-asset-role: guide_map
      description: Registered asset ID with role guide_map.
    h5ad_path:
      type: string
      x-pertura-asset-role: primary_dataset
      description: Registered asset ID with role primary_dataset.
    max_iterations:
      type: integer
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    metadata_barcode_column:
      type: string
    metadata_path:
      type: string
      x-pertura-asset-role: cell_metadata
      description: Registered asset ID with role cell_metadata.
    moi_doublet_path:
      type: string
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    posterior_threshold:
      type: number
    raw_guide_counts_path:
      type: string
      x-pertura-asset-role: guide_matrix
      description: Registered asset ID with role guide_matrix.
    rna_barcode_column:
      type: string
    rna_barcodes_path:
      type: string
      x-pertura-asset-role: rna_barcodes
      description: Registered asset ID with role rna_barcodes.
    tolerance:
      type: number
  additionalProperties: false
  examples:
  - {}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
