schema_version: pertura-capability-spec-v2
capability_id: screen.retained_cells.v1
version: "0.1.0"
phase: 2
kind: diagnostic
summary: Create a deterministic reason-coded retained-cell manifest.
trust_level: exploratory
executor: retained_cells
validator: candidate_standard
input_requirements: [guide_assignment, moi_doublet]
depends_on: [guide.assignment.nb_mixture.v1, screen.moi_doublet.v1, guide.ambient.v1]
dependency_kinds: [contract, guide_assignment, moi_doublet, guide_ambient]
output_kind: retained_cell_manifest
source_class: measured_result
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    max_memory_gb: {type: number, default: 4.0, exclusiveMinimum: 0}
    n_jobs: {type: integer, default: 1, minimum: 1}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
