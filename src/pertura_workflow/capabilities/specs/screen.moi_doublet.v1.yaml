schema_version: pertura-capability-spec-v2
capability_id: screen.moi_doublet.v1
version: "0.1.0"
phase: 2
kind: diagnostic
summary: Profile MOI and transcriptomic doublets as separate phenomena.
trust_level: exploratory
executor: moi_doublet
validator: candidate_standard
input_requirements: [guide_assignment]
depends_on: [guide.assignment.nb_mixture.v1]
dependency_kinds: [contract, guide_assignment]
output_kind: moi_doublet
source_class: measured_result
claim_permissions: []
timeout_seconds: 1800
parameters_schema:
  type: object
  properties:
    max_memory_gb: {type: number, default: 4.0, exclusiveMinimum: 0}
    n_jobs: {type: integer, default: 1, minimum: 1}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
