schema_version: pertura-capability-spec-v2
capability_id: reference.state.control_pca_leiden.v1
version: 1.0.0
phase: 3
kind: analysis
summary: Fit a control-only PCA/Leiden reference and map perturbed cells with frozen
  kNN.
trust_level: builtin_trusted
executor: state_reference
validator: standard
input_requirements:
- dataset_contract
- confirmed_controls
- retained_cells
depends_on:
- diagnostic.guide_assignment.v1
dependency_kinds:
- contract
- retained_cell_manifest
output_kind: state_reference
source_class: measured_result
claim_permissions:
- derived_state_assignment
timeout_seconds: 3600
parameters_schema:
  type: object
  properties:
    chunk_rows:
      type: integer
      minimum: 1
    control_column:
      type: string
    control_values:
      type: array
      items:
        type: string
    h5ad_path:
      type: string
      description: Registered asset ID with role primary_dataset.
      x-pertura-asset-role: primary_dataset
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    resolutions:
      type: array
      items:
        type: string
    seeds:
      type: array
      items:
        type: string
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  deprecated: true
  compatibility_only: true
  dependency_policy:
    diagnostic.guide_assignment.v1:
      scope: exact
      usage: validation_gate
      accepted_statuses:
      - screen_passed
      - caution
