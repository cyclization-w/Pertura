schema_version: pertura-capability-spec-v2
capability_id: diagnostic.design_balance.v1
version: 0.1.0
phase: 1
kind: diagnostic
summary: Check independent units, overlap, estimability and confounding.
trust_level: exploratory
executor: design_balance
validator: candidate_standard
input_requirements:
- dataset_contract
- metadata
depends_on: []
dependency_kinds:
- contract
output_kind: design_balance
source_class: observed_metadata
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    batch_column:
      type: string
    chunk_rows:
      type: integer
      minimum: 1
    condition_column:
      type: string
    donor_column:
      type: string
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    metadata_path:
      type: string
      description: Registered asset ID with role cell_metadata.
      x-pertura-asset-role: cell_metadata
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    paired:
      type: boolean
    replicate_column:
      type: string
    state_column:
      type: string
  required:
  - metadata_path
  additionalProperties: false
  examples:
  - metadata_path: asset_example_cell_metadata
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
