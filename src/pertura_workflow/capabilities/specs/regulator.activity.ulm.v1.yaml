schema_version: pertura-capability-spec-v2
capability_id: regulator.activity.ulm.v1
version: 0.1.0
phase: 6
kind: analysis
summary: Estimate prior-grounded regulator activity with the ULM protocol and a locked
  CollecTRI snapshot.
trust_level: exploratory
executor: regulator_activity_ulm
validator: candidate_standard
input_requirements:
- effect_matrix
- regulator_network
depends_on:
- effect.matrix.assemble.v1
dependency_kinds:
- effect_matrix
output_kind: regulator_activity
source_class: measured_result
claim_permissions: []
timeout_seconds: 3600
parameters_schema:
  type: object
  properties:
    chunk_rows:
      type: integer
      minimum: 1
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    minimum_targets:
      type: integer
    n_jobs:
      type: integer
      default: 1
      minimum: 1
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  derived_only: true
  environment_profile: interpretation-v1
  resource_profile: regulator-knowledge-v1
  dependency_policy:
    effect.matrix.assemble.v1:
      scope: exact
      usage: scientific_input
      accepted_statuses:
      - completed
      - completed_with_caution
