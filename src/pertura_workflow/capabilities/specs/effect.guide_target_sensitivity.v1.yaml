schema_version: pertura-capability-spec-v2
capability_id: effect.guide_target_sensitivity.v1
version: 0.1.0
phase: 5
kind: analysis
summary: Compare guide-level and target-pooled effects with leave-one-guide-out sensitivity.
trust_level: exploratory
executor: guide_target_sensitivity
validator: candidate_standard
input_requirements:
- effect_table
depends_on:
- target.guide_efficacy.v1
dependency_kinds:
- target_guide_efficacy
output_kind: effect_sensitivity
source_class: measured_result
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    chunk_rows:
      type: integer
      minimum: 1
    effect_table_path:
      type: string
      description: Registered asset ID with role effect_table.
      x-pertura-asset-role: effect_table
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    n_jobs:
      type: integer
      default: 1
      minimum: 1
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  dependency_policy:
    target.guide_efficacy.v1:
      scope: exact
      usage: validation_gate
      accepted_statuses:
      - screen_passed
      - caution
