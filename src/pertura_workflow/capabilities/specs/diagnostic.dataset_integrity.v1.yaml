schema_version: pertura-capability-spec-v2
capability_id: diagnostic.dataset_integrity.v1
version: 0.1.0
phase: 1
kind: diagnostic
summary: Check layers, counts, features, barcodes and missingness.
trust_level: exploratory
executor: dataset_integrity
validator: candidate_standard
input_requirements:
- dataset_contract
depends_on: []
dependency_kinds:
- contract
output_kind: dataset_integrity
source_class: observed_metadata
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    cell_column:
      type: string
    chunk_rows:
      type: integer
      minimum: 1
    input_path:
      type: string
      description: Registered asset ID with role primary_dataset.
      x-pertura-asset-role: primary_dataset
    layer:
      type: string
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    modality:
      type: string
    n_jobs:
      type: integer
      default: 1
      minimum: 1
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
