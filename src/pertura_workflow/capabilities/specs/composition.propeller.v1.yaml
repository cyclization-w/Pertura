schema_version: pertura-capability-spec-v2
capability_id: composition.propeller.v1
version: "0.1.0"
phase: 5
kind: analysis
summary: Run replicate-aware cell-state composition analysis with Propeller.
trust_level: exploratory
executor: propeller_composition
validator: candidate_standard
input_requirements: [state_mapping, design_balance]
depends_on: [state.reference.map_knn.v1, diagnostic.design_balance.v1]
dependency_kinds: [contract, state_mapping, design_balance]
output_kind: composition_effect
source_class: measured_result
claim_permissions: []
timeout_seconds: 3600
parameters_schema:
  type: object
  properties:
    max_memory_gb: {type: number, default: 4.0, exclusiveMinimum: 0}
    n_jobs: {type: integer, default: 1, minimum: 1}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  environment_profile: composition-v1
  method_version: speckle-1.10.0
