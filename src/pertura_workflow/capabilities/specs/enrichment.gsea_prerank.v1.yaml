schema_version: pertura-capability-spec-v2
capability_id: enrichment.gsea_prerank.v1
version: 0.1.0
phase: 6
kind: analysis
summary: Run deterministic preranked GSEA through GSEApy with locked gene sets.
trust_level: exploratory
executor: enrichment_gsea_prerank
validator: candidate_standard
input_requirements:
- effect_matrix
- gene_sets
depends_on:
- effect.matrix.assemble.v1
dependency_kinds:
- effect_matrix
output_kind: pathway_enrichment_gsea
source_class: measured_result
claim_permissions: []
timeout_seconds: 7200
parameters_schema:
  type: object
  properties:
    chunk_rows:
      type: integer
      minimum: 1
    max_gene_set_size:
      type: integer
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    min_gene_set_size:
      type: integer
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    permutation_num:
      type: integer
    seed:
      type: integer
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  derived_only: true
  environment_profile: interpretation-v1
  resource_profile: pathway-knowledge-v1
  dependency_policy:
    effect.matrix.assemble.v1:
      scope: exact
      usage: scientific_input
      accepted_statuses:
      - completed
      - completed_with_caution
