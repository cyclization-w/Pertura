schema_version: pertura-capability-spec-v2
capability_id: network.perturbation_regulator.v1
version: 0.1.0
phase: 6
kind: analysis
summary: Build explicitly hypothetical perturbation-to-regulator edges from committed
  activity results.
trust_level: exploratory
executor: perturbation_regulator_network
validator: candidate_standard
input_requirements:
- regulator_activity
depends_on:
- regulator.activity.ulm.v1
dependency_kinds:
- regulator_activity
output_kind: perturbation_regulator_network
source_class: hypothesis
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    activity_threshold:
      type: number
    chunk_rows:
      type: integer
      minimum: 1
    fdr_threshold:
      type: number
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    n_jobs:
      type: integer
      default: 1
      minimum: 1
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  hypothesis_only: true
  dependency_policy:
    regulator.activity.ulm.v1:
      scope: exact
      usage: scientific_input
      accepted_statuses:
      - completed
      - completed_with_caution
