schema_version: pertura-capability-spec-v2
capability_id: program.perturbation.cluster.v1
version: 0.1.0
phase: 6
kind: analysis
summary: Cluster perturbations by committed signed effects with silhouette selection
  and bootstrap stability.
trust_level: exploratory
executor: perturbation_cluster
validator: candidate_standard
input_requirements:
- effect_matrix
depends_on:
- effect.matrix.assemble.v1
dependency_kinds:
- effect_matrix
output_kind: perturbation_clusters
source_class: measured_result
claim_permissions: []
timeout_seconds: 1800
parameters_schema:
  type: object
  properties:
    bootstraps:
      type: string
    chunk_rows:
      type: integer
      minimum: 1
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    seed:
      type: integer
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  derived_only: true
  environment_profile: interpretation-v1
  execution_mode: isolated_python
  dependency_policy:
    effect.matrix.assemble.v1:
      scope: exact
      usage: scientific_input
      accepted_statuses:
      - completed
      - completed_with_caution
