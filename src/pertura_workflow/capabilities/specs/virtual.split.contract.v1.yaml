schema_version: pertura-capability-spec-v2
capability_id: virtual.split.contract.v1
version: 0.1.0
phase: 7
kind: analysis
summary: Freeze non-overlapping perturbation, context, combo, dose-time and donor
  evaluation partitions.
trust_level: exploratory
executor: virtual_split_contract
validator: candidate_standard
input_requirements:
- split_axes
depends_on: []
dependency_kinds: []
output_kind: virtual_split_contract
source_class: observed_metadata
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    axes:
      type: object
    chunk_rows:
      type: integer
      minimum: 1
    heldout_axes:
      type: array
      items:
        type: string
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    module_reference_hash:
      type: string
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    state_reference_hash:
      type: string
  required:
  - axes
  additionalProperties: false
  examples:
  - axes: {}
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
