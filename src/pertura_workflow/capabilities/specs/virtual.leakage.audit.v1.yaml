schema_version: pertura-capability-spec-v2
capability_id: virtual.leakage.audit.v1
version: 0.1.0
phase: 7
kind: virtual
summary: Audit model, state, module and preprocessing contact with the frozen test
  split.
trust_level: exploratory
executor: virtual_leakage_audit
validator: candidate_standard
input_requirements:
- split_contract
- prediction_bundle
depends_on:
- virtual.split.contract.v1
- virtual.prediction.ingest.v1
dependency_kinds:
- virtual_split_contract
- prediction_bundle
output_kind: virtual_leakage_audit
source_class: observed_metadata
claim_permissions: []
timeout_seconds: 900
parameters_schema:
  type: object
  properties:
    chunk_rows:
      type: integer
      minimum: 1
    max_memory_gb:
      type: number
      default: 4.0
      exclusiveMinimum: 0
    module_reference_training_ids:
      type: array
      items:
        type: string
    n_jobs:
      type: integer
      default: 1
      minimum: 1
    preprocessing_training_ids:
      type: array
      items:
        type: string
    state_reference_training_ids:
      type: array
      items:
        type: string
  required: []
  additionalProperties: false
  examples:
  - max_memory_gb: 4.0
implemented: true
metadata:
  validation_status: synthetic_only
  candidate: true
  dependency_policy:
    virtual.split.contract.v1:
      scope: exact
      usage: scientific_input
      accepted_statuses: &id001
      - completed
      - completed_with_caution
    virtual.prediction.ingest.v1:
      scope: exact
      usage: scientific_input
      accepted_statuses: *id001
